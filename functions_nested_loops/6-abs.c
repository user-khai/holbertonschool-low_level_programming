#include "main.h"

/**
 * _abs - returns the absolute value of a number
 * @n: the number the return the absolute value of
 * Return: |n|
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
